<!DOCTYPE html>
<html>
<head>
    <title>Update Password</title>
    <meta charset="utf-8"  content="width=device-width, initial-scale=1.0" name="viewport">
    <link rel="stylesheet" type="text/css" href="/public/update_password.css">
</head>
<body>
    <% var u;%>
    <% if(u==1){%>
        <script>alert("Incorrect Credentials !!");</script>
        <%}%>
        <% if(u==2){%>
            <script>alert("Passwords do not match !!");</script>
            <%}%>
<div class="registrationbox">
    <!-- <div ><img src="photos/user.png" id="user">     -->
<h1 style="text-align:center;font-family:cursive;";>Update Password</h1>
<form method="post" action="/users/update_password"  >
        <label for="email">Email id :</label>
        <input autocomplete="off" type="email" title="Enter your email" placeholder="abcd@gmail.com" name="email" id="email" required>
        <br>
        <br>
        <label for="phone">Username : </label>
        <input type="text" name="user_name" placeholder="User_name" autocomplete="off"  required><br><br>

        <label for="pass1">New Password : </label>
        <input id="pass" type="password" name="new_pass" placeholder="Aa123456" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9]).{8,}" title="Must contain (A-Z),(a-z),(0-9); min. 8 characters" required><br><br>

        <label for="pass2">Confirm Password : </label>
        <input id="cnf" type="password" name="pass_chck" placeholder="Re-enter new Password" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" title="Confirm New Password" required>
        <br>
        <br>
        <br>
        <input type="submit" name="submit" value="Update" class="size">
    </form>
</body>
</html>
